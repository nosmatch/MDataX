<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mogu.data.integration.mapper.TaskSyncDetailMapper">

    <select id="selectByTaskId" resultType="com.mogu.data.integration.entity.TaskSyncDetail">
        SELECT *
        FROM task_sync_detail
        WHERE task_id = #{taskId}
    </select>

    <update id="updateByTaskId">
        UPDATE task_sync_detail
        SET source_datasource_id = #{sourceDatasourceId},
            source_table = #{sourceTable},
            target_datasource_id = #{targetDatasourceId},
            target_table = #{targetTable},
            sync_type = #{syncType},
            time_field = #{timeField},
            where_condition = #{whereCondition},
            update_time = NOW()
        WHERE task_id = #{taskId}
    </update>

</mapper>
