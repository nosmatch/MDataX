# 一、整体产品结构（信息架构）

先把产品“骨架”定死：

```id="1f0n2p"
首页（工作台）
├── 数据集成
│    ├── 数据源管理
│    ├── 同步任务
│
├── 数据开发
│    ├── SQL查询            ← 左侧可读表，即席查询
│    ├── SQL开发            ← 右侧可写表，保存/调度
│    └── 任务管理
│
├── 数据资产
│    ├── 数据目录
│    ├── 表详情
│
├── 权限管理
│    ├── 用户管理
│    ├── 角色管理
│    ├── 权限配置
│
└── 系统管理
     ├── 操作日志
     ├── 系统设置
```

👉 原则：
**左侧导航 + 右侧工作区（标准B端设计）**

---

# 二、全局布局设计

```id="v7rj2s"
┌──────────────────────────────┐
│ 顶部导航（Logo / 用户 / 环境） │
├──────────────┬───────────────┤
│ 左侧菜单      │ 主工作区        │
│              │               │
│ 数据集成      │ 页面内容        │
│ 数据开发      │               │
│   ├ SQL查询   │               │
│   ├ SQL开发   │               │
│   └ 任务管理  │               │
│ 数据资产      │               │
│ 权限管理      │               │
└──────────────┴───────────────┘
```

---

# 三、核心页面原型（逐个拆）

---

# 1️⃣ 首页（工作台）

## 🎯 目标

让用户一进来就知道“数据平台在干嘛”

---

## 页面结构

```id="ib96kx"
[数据概览]
- 表数量
- 今日任务数
- 成功率

[最近任务]
- 任务名 / 状态 / 时间

[最近访问数据]
- 表名 / 访问次数

[快捷入口]
- 新建同步任务
- 新建SQL任务
```

---

# 2️⃣ 数据集成模块

---

## 2.1 数据源管理

### 页面结构

```id="s4l8nf"
[数据源列表]
--------------------------------
名称 | 类型 | 状态 | 操作
--------------------------------
mysql_order | MySQL | 正常 | 编辑/删除

[按钮]
+ 新建数据源
```

---

### 新建数据源弹窗

```id="q6bl4x"
类型：MySQL
Host：
Port：
数据库：
用户名：
密码：
[测试连接]
[保存]
```

---

---

## 2.2 同步任务

---

### 页面结构

```id="px1q6q"
[任务列表]
-----------------------------------------
任务名 | 来源表 | 目标表 | 类型 | 状态
-----------------------------------------

[按钮]
+ 新建同步任务
```

---

### 新建同步任务

```id="ewm0iq"
步骤1：选择数据源
步骤2：选择表
步骤3：配置规则
    - 全量 / 增量
    - 时间字段
步骤4：调度（可选）

[保存并运行]
```

---

# 3️⃣ 数据开发模块

---

数据开发模块包含两个核心工作页面：**SQL查询**和**SQL开发**，两者各自独立入口，不共用同一页面。

| 页面 | 权限维度 | 表结构展示位置 | 核心能力 | 目标用户 |
|------|---------|--------------|---------|---------|
| SQL查询 | 读权限 | **左侧** | 即席执行、只读限制、快速预览 | 分析师/业务人员 |
| SQL开发 | 写权限 | **右侧** | 保存任务、定时调度、运行记录 | 数据工程师 |

---

## 3.1 SQL查询

---

### 页面结构

```id="3mkj6z"
左侧：
[可读表列表]          ← 根据用户读权限过滤
- 表名
- 展开显示字段列表

中间：
[SQL编辑器（Monaco Editor）]

顶部：
[执行] [限制条数：100/500/1000]

底部：
[查询结果表格]
- 显示执行时间
```

---

👉 实际查询 ClickHouse；左侧只展示用户有**读权限**的表；SQL仅限 SELECT，默认限制100条，超时 < 5秒

---

## 3.2 SQL开发

---

### 页面结构（重点）

```id="gk7w2m"
顶部：
[任务名称] [保存] [运行]

中间：
[SQL编辑器]
--------------------------------
SELECT * FROM xxx
--------------------------------

右侧：
[可写表列表]          ← 根据用户写权限过滤
- 表名
- 展开显示字段列表

[调度配置]
- Cron表达式
- 目标表（可选）

底部：
[执行结果]
- 表格展示
- 执行时间
- Tab切换：结果 / 运行记录
```

---

👉 右侧只展示用户有**写权限**的表；支持保存任务、定时调度、运行记录；类似简化版 DataWorks

---

## 3.3 任务管理

---

```id="5r0v1k"
[任务列表]
-----------------------------------------
任务名 | 调度 | 状态 | 最近运行时间
-----------------------------------------

[操作]
- 编辑
- 运行
- 查看日志
```

---

# 4️⃣ 数据资产模块（很关键）

---

## 4.1 数据目录

---

### 页面结构

```id="u8t6yo"
[搜索框]

[表列表]
-----------------------------------------
表名 | 所属库 | 描述 | 创建时间
-----------------------------------------

左侧筛选：
- 数据库
- 创建人
```

---

---

## 4.2 表详情页（核心）

---

```id="7y7n8m"
顶部：
表名 + 描述 + Owner

Tab页：

[1] 字段信息
--------------------------------
字段名 | 类型 | 描述
--------------------------------

[2] 基本信息
- 创建时间
- 数据量（可选）

[3] 权限信息
- 谁可以访问

[按钮]
申请权限（预留）
```

---

# 5️⃣ 权限管理模块

---

## 5.1 用户管理

```id="h4yzql"
用户列表：
--------------------------------
用户名 | 角色 | 状态
--------------------------------

[按钮]
+ 新建用户
```

---

---

## 5.2 角色管理

```id="p5m1b9"
角色列表：
--------------------------------
角色名 | 描述
--------------------------------

[按钮]
+ 新建角色
```

---

---

## 5.3 权限配置（核心）

---

```id="0n1hcd"
选择角色：

[表权限]
--------------------------------
表名 | 权限（读/写）
--------------------------------

[保存]
```

---

👉 权限绑定元数据

---

# 6️⃣ 系统管理（简单版）

---

## 6.1 操作日志

```id="o3y5kn"
时间 | 用户 | 操作 | 结果
```

---

# 四、关键交互流程（必须打通）

---

## 🔁 流程1：数据接入

```id="ppf8q5"
数据源 → 创建同步任务 → 数据进入 ClickHouse
```

---

## 🔁 流程2：数据开发

```id="y1j6bo"
SQL开发页面 → 编写SQL → 保存任务 → 调度 → 生成新表
```

---

## 🔁 流程3：数据查询（即席）

```id="v9i8bt"
搜索表 → 查看结构 → 数据开发 / SQL查询（即席模式）
```

---

## 🔁 流程4：权限控制

```id="w2a6rf"
用户 → 角色 → 表权限 → 查询校验
```

---

# 五、设计原则（帮你避免做烂）

---

### ✅ 1. 极简优先

不要一上来做：

* 血缘图
* 拖拽ETL

---

### ✅ 2. SQL优先

👉 数据人更喜欢 SQL，不是拖拽

---

### ✅ 3. 所有入口围绕“表”

👉 表 = 数据资产核心

---

### ✅ 4. 页面不要超过3层

否则用户迷路

---

# 六、一句话总结

👉 这个原型的本质是：

**“围绕表，打通 数据接入 → 开发 → 管理 → 查询 → 权限 的最短路径”**

