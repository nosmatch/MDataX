# 📄 数据管理平台 PRD（MVP版本）

---

# 一、产品背景

## 1.1 背景问题

当前企业数据存在典型问题：

* 数据分散（MySQL / 日志 / 手工报表）
* 数据获取成本高（依赖研发）
* 数据口径不统一
* 权限混乱（谁都能查 or 谁都查不了）

---

## 1.2 产品目标

构建一个轻量级数据平台，实现：

* 数据统一接入（MySQL → ClickHouse）
* 数据开发能力（SQL + 调度）
* 数据资产可发现（元数据）
* 数据权限可控

---

## 1.3 MVP成功标准（必须量化）

上线后满足：

* ≥80% 数据分析需求无需找研发
* 核心数据表100%纳入管理
* 数据查询响应 < 5秒
* 无越权访问

---

# 二、用户角色

| 角色    | 说明   | 核心诉求    |
| ----- | ---- | ------- |
| 数据分析师 | 使用数据 | 找数据、查数据 |
| 数据工程师 | 管数据  | 建表、开发任务 |
| 业务人员  | 看结果  | 查询报表    |
| 管理员   | 管权限  | 控制访问    |

---

# 三、产品范围（MVP）

---

## ✅ 包含模块

1. 数据集成
2. 数据开发
3. 数据资产（元数据）
4. 权限管理
5. 数据查询

---

## ❌ 不包含（后续阶段）

* 实时计算（如 Apache Flink）
* 字段级血缘
* 数据质量体系
* 数据标签体系（复杂版）

---

# 四、产品整体结构

```id="d2o4q3"
数据接入 → 数据开发 → 数据存储（ClickHouse） → 数据资产 → 数据查询
                                  ↓
                               权限控制
```

---

# 五、功能设计（核心）

---

## 5.1 数据集成模块

### 🎯 目标

实现 MySQL 数据自动进入平台

---

### 功能点

#### 1️⃣ 数据源管理

* 支持类型：

  * MySQL
* 配置项：

  * host / port / db / user / password

---

#### 2️⃣ 同步任务

支持：

* 全量同步
* 增量同步（基于时间字段）

---

### 用户流程

```id="ibgq9u"
创建数据源 → 选择表 → 配置同步规则 → 启动任务
```

---

### 验收标准

* 可创建同步任务
* 数据自动写入 ClickHouse
* 支持任务失败重试

---

## 5.2 数据开发模块

---

### 🎯 目标

让用户能写 SQL 处理数据

---

### 功能点

#### 1️⃣ SQL编辑器

* 支持 SQL 编写
* 支持执行

---

#### 2️⃣ 任务管理

* 创建任务
* 定时执行（cron）
* 手动执行

---

#### 3️⃣ 运行记录

* 成功 / 失败状态
* 执行时间
* 日志查看

---

### 执行引擎

* 基于 Apache Airflow

---

### 验收标准

* SQL任务可执行
* 调度任务稳定运行

---

## 5.3 数据资产模块（元数据）

---

### 🎯 目标

让用户“找得到数据、看得懂数据”

---

### 功能点

#### 1️⃣ 数据目录

展示：

* 表名
* 描述
* 所属库
* 创建时间

---

#### 2️⃣ 字段信息

* 字段名
* 类型
* 描述

---

#### 3️⃣ 搜索能力

支持：

* 按表名搜索

---

### 数据来源

自动解析 ClickHouse 表结构

---

### 验收标准

* 所有表自动入库
* 可搜索数据

---

## 5.4 权限管理模块

---

### 🎯 目标

控制谁能访问哪些数据

---

### 功能点

#### 1️⃣ 用户管理

* 创建用户

---

#### 2️⃣ 角色管理

* 创建角色

---

#### 3️⃣ 权限控制

* 表级权限：

  * 读
  * 写

---

### 权限模型

RBAC：

```id="z0sv7k"
用户 → 角色 → 表权限
```

---

### 验收标准

* 无权限用户无法查询数据
* 权限实时生效

---

## 5.5 数据查询模块

---

### 🎯 目标

提供统一查询入口

---

### 功能点

* SQL查询
* 表数据预览（前100条）

---

### 数据源

* ClickHouse

---

### 验收标准

* 查询响应 < 5秒
* 权限校验生效

---

# 六、非功能需求

---

## 6.1 性能

* 单次查询 < 5秒
* 支持并发 20 用户

---

## 6.2 稳定性

* 调度成功率 ≥ 99%
* 数据同步失败可重试

---

## 6.3 安全

* 所有查询必须权限校验
* 操作日志记录

---

# 七、关键流程

---

## 7.1 数据接入流程

```id="fx6vql"
MySQL → 数据集成 → ClickHouse
```

---

## 7.2 数据开发流程

```id="5h4m6c"
SQL开发 → Airflow调度 → 生成结果表
```

---

## 7.3 数据查询流程

```id="91rwqp"
用户查询 → 权限校验 → ClickHouse → 返回结果
```

---

# 八、风险与约束

---

## ⚠️ 风险

1. ClickHouse 表设计不合理 → 查询慢
2. Airflow 使用复杂 → 任务不稳定
3. 权限设计不足 → 数据泄露

---

## 📌 约束

* 数据量：GB级
* 技术优先轻量化
* 3个月内必须上线

---

# 九、版本规划

---

## V1（当前）

* 数据接入
* SQL开发
* 元数据（表级）
* 权限（表级）

---

## V2（后续）

* Kafka接入
* 数据血缘
* 字段级权限

---

# 十、一句话总结

👉 这个MVP的本质是：

**“让数据从数据库流入平台，并能被安全地查询和使用”**
